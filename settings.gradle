dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        // 🔥 This is what tells Gradle to look in /libs
        flatDir {
            dirs 'libs'
        }
    }
}
