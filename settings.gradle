dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        flatDir {
            dirs 'libs'  // This matches your 'libs' folder where .aar is placed
        }
    }
}
